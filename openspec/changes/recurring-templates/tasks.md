## Tasks

- [x] 11.1 Schema and repository - `recurring_templates` table (schemaVersion 13→14) with name, direction, financial account, category, amount, day-of-month, and a `lastRecordedYearMonth` marker; `LedgerRepository` CRUD plus `watchDueRecurringTemplates()` (joined with account/category names and currency) and `recordDueTemplate()` (posts via the existing `recordTransaction`, then stamps the month). Pure due-date logic (`effectiveDayOfMonth`/`isTemplateDue`/`yearMonthOf`) lives in the domain model, framework-free.
- [x] 11.2 Template CRUD UI - `RecurringTemplateManagementViewModel`/`View` (add/edit/delete, real delete since nothing references a template from a journal entry), reachable via a "Manage recurring templates" button in Settings → `/recurring-templates` route.
- [x] 11.3 Due today surfacing - `HomeViewModel.dueTemplates`, a "DUE TODAY" section on `home_view.dart` above Pending Transfers; tapping calls `recordDueTemplate` directly (one tap, no confirmation dialog - the tap itself is the confirmation, matching "does not auto-post without tap").
- [x] 11.4 Tests - `recurring_template_test.dart` (pure due-date logic, including a day-31 template clamping to Feb 28), `ledger_repository_test.dart`'s "recurring templates" group (CRUD, validation, due-list join, recordDueTemplate posting + removing from due list), `home_view_model_test.dart`/`home_view_test.dart` (due section render + tap), `recurring_template_management_view_model_test.dart`/`_view_test.dart` (add/delete flows), and a schemaVersion-13→14 migration test.
