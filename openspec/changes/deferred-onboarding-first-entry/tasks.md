## Tasks

- [x] 1.1 `app_router.dart`: change the redirect guard so a first-run session (identity exists, unacknowledged) reaches the currency and first-entry screens before the recovery-phrase redirect fires; the redirect still fires unconditionally once that first entry has posted, or on any relaunch before acknowledgment. Required a schema addition beyond what design.md originally scoped: identity existence alone can no longer signal "onboarded," since the identity is now committed before acknowledgment - added `signing_identities.acknowledgedAt` (nullable, schemaVersion 12, backfilled from `createdAt` for every pre-existing identity so upgrading users are never sent back through acknowledgment for a phrase this app never stored).
- [x] 1.2 Currency screen (reused existing `CurrencySelectionView`, now first) and a minimal guided first-entry screen (`RecordTransactionView` directly, per the "thin wrapper" option) reachable only in this pre-acknowledgment window. The starter account keeps its existing fixed name (`Cash & Bank`) rather than adding a free-text naming field here - that UI belongs to `first-week-setup-wizard`'s starter-chart-of-accounts prompt, not duplicated here.
- [x] 1.3 Confirm the guided first entry calls the same `recordTransaction` path as every other entry — no new posting method, no bypass of the existing "identity must exist" guard.
- [x] 1.4 After the first entry posts, force the existing recovery-phrase acknowledgment route; verified it blocks navigation to anything else, including app resume after backgrounding/kill (redirect re-evaluates `acknowledgedAt` on every navigation, not just once).
- [x] 1.5 Migrated existing onboarding view-model/view tests for the new flow (`commitIdentity`/`acknowledge` replacing `finishOnboarding`); added migration tests for the `acknowledgedAt` backfill and repository tests for post-upgrade commit behavior; added a view-model test for resuming a stashed phrase after a simulated kill (`resumePendingIdentity`). Not covered: a full GoRouter-level widget test of the kill-and-relaunch redirect sequence - this repo has no existing `app_router_test.dart` harness to extend, and the underlying invariant (redirect re-derives purely from `identity`/`acknowledgedAt`/`hasAnyJournalEntries`, not in-memory state) is covered at the repository/view-model level instead.
- [x] 1.6 Update user guide onboarding order.
