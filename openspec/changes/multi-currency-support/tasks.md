1## 1. Schema and domain model

- [x] 1.1 Add `currency` column to `account_groups` (ISO 4217 code, `TEXT NOT NULL`); require it whenever a group is created
- [x] 1.2 Add the system "Transfers-in-transit" account (`group_id = NULL`, never user-selectable), seeded alongside the existing `equity` row in `confirmFirstIdentity` and the matching `onUpgrade` path
- [x] 1.3 Add `pending_transfers` Drift table (`id`, `kind`, `source_account_id`, `category_id`, `destination_account_id`, `provisional_entry_id`, `status`, `settlement_entry_id`, `fee_entry_id`, `initiated_at`, `settled_at`); `kind` (`PendingTransferKind { transfer, foreignTransaction }`) and `status` (`PendingTransferStatus { pending, settled }`) are real Dart enums via `textEnum<T>()`, per Golden Rule #5 — not raw strings
- [x] 1.4 Bump `schemaVersion` to 4 (multi-account-support is 3). Write both `onCreate` (schema-only; groups are seeded with currency by `confirmFirstIdentity`, chosen during a new onboarding step — see design.md addendum) and `onUpgrade(3, 4)` (add `account_groups.currency` nullable, `pending_transfers` table, seed the Transfers-in-transit account; existing groups land with `currency = NULL`, which is the signal for the app-level one-time currency-backfill prompt)
- [x] 1.5 Add domain models: `AccountGroup.currency`, `PendingTransfer` (no separate settlement DTOs — `settlePendingTransfer` takes named parameters directly, matching `createFinancialAccount`'s existing convention)
- [x] 1.6 Add or extend domain exceptions: reused `AccountGroupException` for group-currency mismatch/change-while-assigned/cross-currency-reassignment (same family as its existing "not found"/"kind mismatch" cases); added `PendingTransferException` covering every settlement/pending-transfer violation (negative/exceeding amount, already-settled, invalid fee category, invalid settlement target, direct reversal of a still-pending provisional entry)

## 2. Repository: currency, provisional posting, settlement

- [x] 2.1 Enforce a currency on every account group at creation; require group assignment to resolve a financial account's display currency
- [x] 2.2 Reject changing a group's currency while it has at least one active financial account; allow it when the group has zero active accounts
- [x] 2.3 Extend `recordTransfer` to branch on currency: same-currency (unchanged from multi-account-support), known-rate cross-currency (single complete entry, two currencies, no `pending_transfers` row — reject if either currency's amount is non-positive), unknown-rate cross-currency (provisional entry against the Transfers-in-transit account + new `pending_transfers` row)
- [x] 2.4 Extend `recordTransaction` with the same branching for a foreign-currency transaction against a financial account whose group currency differs from the transaction's native currency — the category leg always posts immediately in the native currency; reject a known-rate entry if either currency's amount is non-positive
- [x] 2.5 Implement `settlePendingTransfer({pendingTransferId, settledToAccountId, settledAmountMinor, feeCategoryId?})` with the corrected shortfall-comparable-only-for-transfer-to-source logic; also rejects a zero settlement on the non-shortfall path (caught during apply — it would otherwise leave the Transfers-in-transit position permanently unclosed)
- [x] 2.6 Implement a pending-transfers query for the Home overview — one row per unsettled item, ordered by `initiated_at`
- [x] 2.7 Update the home overview / net-worth query to compute net position per currency instead of one blended figure, including each pending item's amount in its source currency's net position (excluding a pending transfer whose provisional entry is quarantined or migration-superseded, same as any other excluded posting); label every group total, account balance, and pending-transfer amount with its currency
- [x] 2.8 Ensure the Transfers-in-transit account is excluded from every financial-account picker, the account-management list, and the home overview's account/group rows — allowlist the pickers to real financial account types, don't rely on excluding it by name (a new `AccountType.clearing` value, not asset/liability, so the existing allowlists exclude it automatically)
- [x] 2.9 Route every new write (provisional entry, settlement entry, fee entry) through the same signed/chained write path as `recordTransaction`/`recordTransfer`/`reverseEntry` — no separate ad hoc write mechanism
- [x] 2.10 Reject reassigning a financial account to an account group with a different currency than its current group (extends `multi-account-support`'s existing group-reassignment API)
- [x] 2.11 Reject a direct `reverseEntry` call against an entry that is still the open `provisional_entry_id` of a `pending` pending transfer; point the caller at `settlePendingTransfer` instead. Once settled, reversal works normally.

## 3. Unit tests (repository / domain)

- [x] 3.1 Tests: creating a group requires a currency; an account's display currency is derived from its group
- [x] 3.2 Tests: group-currency change rejected with active accounts assigned; allowed when the group is empty
- [x] 3.3 Tests: same-currency transfer behavior is unchanged (regression against `multi-account-support`'s existing transfer tests)
- [x] 3.4 Tests: known-rate cross-currency transfer posts one complete entry, no `pending_transfers` row is created; a zero or negative amount in either currency is rejected
- [x] 3.5 Tests: unknown-rate cross-currency transfer posts a provisional entry and creates a `pending_transfers` row with status pending
- [x] 3.6 Tests: foreign-currency transaction posts the category leg immediately in its native currency, posts a provisional account leg, creates a pending row
- [x] 3.7 Tests: settlement to the original destination in the destination currency closes the pending transfer with no shortfall comparison and no fee entry, even when the destination-currency amount is numerically less than the source-currency provisional amount
- [x] 3.8 Tests: settlement to the source account for less than the original amount (same currency) posts a fee entry for the shortfall and fully closes the position
- [x] 3.9 Tests: settlement with zero amount returned to the source account posts the full original amount as a fee/loss entry
- [x] 3.10 Tests: a `feeCategoryId` supplied together with a destination-account settlement is rejected; the source-account shortfall invariant (settlement + fee amounts exactly equal the provisional amount) holds whenever it applies; a source-account settlement exceeding the provisional amount is rejected; settling against an account that has since been archived still succeeds
- [x] 3.11 Tests: the Transfers-in-transit account never appears in any picker and has no single-balance query exposed for it
- [x] 3.12 Tests: per-currency net position calculation across multiple currencies, including a pending item's amount counted in its source currency's net position; every displayed total/balance is labeled with its currency
- [x] 3.13 Tests: quarantined and migration-superseded entries remain correctly excluded under per-currency net position (regression against `ledger-integrity-signing`'s exclusion rules), including a quarantined or superseded pending transfer's provisional entry — excluded from net worth, still visible in the Pending Transfers section
  - All of 3.1-3.13 added to `test/data/repositories/ledger_repository_test.dart` (new groups after the pre-existing ones). Also fixed 5 stale `confirmFirstIdentity(generated)` call sites (missing the now-required `currency:` param) and 3 `watchHomeOverview` assertions still using the old blended `HomeOverview.totalAssetsMinor`/etc. fields, which were breaking compilation of the entire file before any new tests could even run.
- [x] 3.14 Tests: `onCreate` and `onUpgrade` migration paths, including the currency-backfill prompt path for pre-existing `account_groups` rows (mirror `test/data/database/app_database_migration_test.dart`'s hand-built-old-schema approach)
  - Added a `_openV3Database()` fixture (multi-account-support's shipped schema, no `currency`/`pending_transfers`/clearing account) and 4 new tests: currency lands `NULL` post-upgrade (`needsCurrencyBackfill` true), the clearing account seeds correctly and is never in the financial-account picker, `backfillGroupCurrencies` fills every group, and `pending_transfers` is queryable.
  - **Real bug found and fixed**: running these against the *actual* migration code (not just new tests) surfaced that a database skipping straight from schemaVersion < 3 to 4 crashed with "duplicate column name: currency" - `if (from < 3) { m.createTable(accountGroups); ... }` creates the table from its *current* Dart definition (already including `currency`, since Drift generates columns from the live class, not a versioned snapshot), then `if (from < 4)` unconditionally tried to `addColumn` the same column again. Fixed in `app_database.dart` by only running that `addColumn` when `from >= 3` (i.e. `account_groups` already existed before this migration call). `onCreate` is exercised implicitly by every other repository test's `setUp` (fresh in-memory DB + `confirmFirstIdentity(currency: ...)`).
- [x] 3.15 Tests: reassigning a financial account to a different-currency group is rejected; reassigning within the same currency still works (regression against `multi-account-support`'s existing reassignment tests)
- [x] 3.16 Tests: negative settled amount rejected; settling an already-settled pending transfer rejected; a fee category that isn't an active Expense category rejected
- [x] 3.17 Tests: `settlePendingTransfer` on a `foreignTransaction` pending item always resolves to its own `source_account_id` regardless of a different `settledToAccountId` supplied, follows the no-shortfall destination-delivery path (not the shortfall-comparable one), and rejects a `feeCategoryId` if supplied
- [x] 3.18 Tests: directly reversing a still-pending provisional entry is rejected; reversing it after settlement succeeds normally

## 4. UI: recording flows

- [x] 4.1 Update Transfer UI/ViewModel to detect a cross-currency transfer and branch between a known-rate form (both amounts entered) and an unknown-rate form (source amount only, posts provisional)
  - `TransferViewModel` now subscribes to `watchAccountGroups()`, exposes `currencyFor(accountId)` and `isCrossCurrency`, and a `destinationAmountMinor` field threaded into `recordTransfer` only when cross-currency.
  - `TransferView` shows currency suffixes on the amount field and conditionally renders an optional "Destination amount" field with helper text when cross-currency, leaving it blank for the provisional/unknown-rate path.
- [x] 4.2 Update Record Transaction UI/ViewModel with the same branching when the selected account's group currency differs from the transaction's native currency
  - `RecordTransactionViewModel` gained `currencyFor`/`accountCurrency`, a settable `nativeCurrency` override (null = same as account), `isForeignCurrency`, and `accountCurrencyAmountMinor`, threaded into `recordTransaction`.
  - `RecordTransactionView` adds an optional "Transaction currency" field and, when foreign, an optional "Account-currency amount" field (blank = provisional).
- [x] 4.3 Add a Settle Pending Transfer UI/ViewModel: choose the account that actually received funds, enter the real settled amount, choose an expense category if there's a shortfall to cover
  - New `lib/ui/features/settle_pending_transfer/` (ViewModel + View), wired via `/settle-pending-transfer/:pendingTransferId` in `app_router.dart`; `HomeView.onSettlePendingTransfer` now navigates there, looking up the tapped item's `PendingTransferSummary` from `HomeViewModel.overview`.
  - **Bug found and fixed while building this**: `pending_transfers` had no `currency` column, so `_buildHomeOverview` derived the display currency from the source account's own group - correct for `transfer` but wrong for `foreignTransaction`, whose provisional clearing-leg amount is in the transaction's *native* currency (design.md Decision 7), not the account's currency. Added `pending_transfers.currency`, snapshotted at provisional-entry creation (`_postProvisionalEntry`), and `PendingTransfer.currency`; `_buildHomeOverview` and the Settle screen now read it directly instead of re-deriving it.
- [x] 4.4 Add currency selection to account-group creation/edit UI; disable changing it once the group has active accounts
  - Groups aren't user-creatable (fixed starter groups seeded at onboarding, per `_seedSystemGroupsEquityAndClearing`) - so this became the group edit dialog: `_showRenameGroupDialog` in `account_management_view.dart` now edits name and currency together, wired to the new `AccountManagementViewModel.changeGroupCurrency` -> `LedgerRepository.changeAccountGroupCurrency`. The currency field disables when the group has any active (non-archived) account.

## 5. UI: home overview

- [x] 5.1 Update Home overview to show net position per currency instead of one blended figure; label every group total and account balance with its currency
  - `HomeView`'s `_NetPositions` iterates `overview.netPositionsByCurrency`; `_GroupSection` labels every group total and account balance with `section.group.currency`.
- [x] 5.2 Add a Pending Transfers section to Home overview — one line item per pending item, tappable to open the settle flow, amount labeled with its currency
  - `HomeView`'s `_PendingTransfers` section is now wired to `onSettlePendingTransfer`, which `app_router.dart` routes to `/settle-pending-transfer/:pendingTransferId` (task 4.3).
- [x] 5.3 Confirm the Transfers-in-transit account never appears in the Home overview's account or group rows
  - `watchFinancialAccounts()` allowlists `asset`/`liability` only, and `_buildHomeOverview`'s sections loop only includes accounts with a `groupId` (the clearing account's is `NULL`) - `AccountType.clearing` is excluded from both by construction, not by name.
- [x] 5.4 Add a currency constraint to the account-group reassignment picker (Account management, from `multi-account-support`'s Decision 1/2.9) — only offer same-currency groups as reassignment targets
  - `_showReassignDialog` in `account_management_view.dart` now filters candidate groups to the account's current group's currency, in addition to the existing `kind` filter. The Repository (`reassignFinancialAccountGroup`) already rejected cross-currency reassignment server-side; this closes the same gap in the picker itself.

## 6. Widget / integration coverage and polish

- [x] 6.1 Widget tests: per-currency net position display; Pending Transfers section rendering
  - Added to `test/ui/features/home/views/home_view_test.dart`: rewrote the two existing tests for the new per-currency `HomeOverview` shape, plus a new test rendering the Pending Transfers section and verifying a tap invokes `onSettlePendingTransfer`.
- [x] 6.2 Widget tests: Transfer UI branching (same-currency / known-rate / unknown-rate) and the Settle UI
  - `test/ui/features/transfer/views/transfer_view_test.dart`: new `cross-currency branching` group (4 tests) covering same-currency hides the destination-amount field, cross-currency shows it, blank posts provisional, filled posts known-rate.
  - New `test/ui/features/settle_pending_transfer/views/settle_pending_transfer_view_test.dart` (3 tests): transfer-to-destination (no fee), transfer-to-source with a shortfall (fee category required and passed through), and foreignTransaction (no account picker, always settles to its own source).
- [x] 6.3 Integration test: full cross-currency transfer lifecycle — initiate provisional, verify it appears pending on Home, settle it, verify net position moves from the source currency to the destination currency
- [x] 6.4 Integration test: bounced transfer with a retained fee — initiate, settle back to the source account for less than sent, verify the fee posts and the pending item clears
  - Both added to `integration_test/app_test.dart`, driving the real widget tree (Accounts → Transfer → Home → Settle) - all 10 integration tests pass on `flutter test integration_test/app_test.dart -d macos`.
  - **Pre-existing environment bug found and fixed**: this macOS desktop target's default test-surface size didn't hit-test correctly against `AppShell`'s bottom navigation bar (`tap()` "derived an Offset that would not hit test"), breaking any test that taps a nav-bar tab early - including a completely unrelated, pre-existing test (`record money in updates the register and running balance`). Added a `pumpApp()` helper that pins the test surface to a phone-sized 400x900 @ 1x before every `pumpWidget`, fixing it for the whole file.
  - **Also fixed a stale pre-existing test**: `reinstall with the recovery phrase restores the identity without re-signing` tapped "Confirm" and waited directly for Home, from before this change added the currency-selection step in between (design.md addendum) - updated it to walk through "Choose your currency" → "Finish setup" first.
  - **Also fixed a real UI regression this change introduced**: `RecordTransactionView`'s new "Transaction currency" field was inserted *before* the "Amount" field, silently breaking the `find.byType(TextField).first` convention several tests rely on to mean "the amount field". Reordered so Amount comes first (also better UX - currency is a refinement of the amount, not vice versa).
- [x] 6.5 Run `dart analyze` and fix issues introduced by this change
  - `flutter analyze` (whole repo): "No issues found!" Also fixed several pre-existing compile breaks left over from the earlier (pre-this-session) part of the apply: 5 stale `confirmFirstIdentity(generated)` call sites across `test/data/repositories/ledger_repository_test.dart`, `test/data/database/app_database_migration_test.dart`, and `integration_test/app_test.dart` (all missing the now-required `currency:` param), stale `HomeOverview.totalAssetsMinor`/etc. assertions, and two onboarding tests written against `RecoveryPhraseSetupViewModel.confirm`'s old async/committing signature (now sync/non-committing, with a separate `finishOnboarding`).
- [ ] 6.6 Manual smoke: migrate an existing `multi-account-support` database, confirm the currency backfill prompt, create a second-currency group, verify transfer and settlement flows end to end
