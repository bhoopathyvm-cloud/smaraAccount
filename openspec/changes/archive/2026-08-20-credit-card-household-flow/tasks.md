## Tasks

- [x] 1.1 Schema: `isCreditCard` boolean on liability accounts, set at creation, immutable, rejected for asset accounts - `accounts.is_credit_card` (schemaVersion 15→16, plain `addColumn`); `createFinancialAccount` rejects `isCreditCard: true` for a non-liability type; no update path ever writes it, so it's immutable by omission (mirrors `holdsInvestments`).
- [x] 1.2 Record-transaction capture: "Paid from card" / "Paid from bank" shortcuts when at least one card-flagged account exists; both resolve to the existing account picker, pre-filtered - `RecordTransactionViewModel.selectPaidFromCard`/`selectPaidFromBank`/`financialAccountOptions`, two `ChoiceChip`s shown only for Spent with `hasCardAccounts`; changing direction clears the filter so it never silently narrows the picker once the chips are hidden.
- [x] 1.3 "Pay card" action: pre-fills the existing transfer flow (bank source, card destination), no new repository method - `TransferViewModel` gained `initialToAccountId` (same still-active-by-arrival safety as the existing `initialFromAccountId`); a "Pay card" button on `RegisterView` (shown for an active credit-card account, alongside the existing closeout-button pattern) pushes `/transfer?toAccountId=...`.
- [x] 1.4 Home: style card-flagged liability rows distinctly (label/icon) — the balance figure itself is already shown by existing per-account display; no new data needed - a `TablerIcons.creditCard` leading icon on `_GroupSection`'s account rows, in place of the ordinary wallet icon.
- [x] 1.5 Tests: card flag immutable and asset-rejected; "Paid from card" posts an ordinary expense against the card; "Pay card" posts an ordinary transfer and reduces the card's amount owed - `ledger_repository_test.dart`'s credit-card tests in the "financial account management" group (create, immutability via rename, asset rejection) plus a schemaVersion-15→16 migration test; `record_transaction_view_model_test.dart`/`_view_test.dart`'s "credit-card-household-flow" groups (shortcut narrowing, direction-change reset, no shortcuts without a card); `transfer_view_model_test.dart`'s `initialToAccountId` group; `register_view_test.dart`'s "Pay card" group; `account_management_view_test.dart`'s checkbox group.
- [x] 1.6 User guide: marking a card, the paid-from shortcut, and Pay card - Accounts and account groups section (the checkbox) and a new "Credit cards" subsection under Transfers.
