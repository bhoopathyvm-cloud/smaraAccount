## Tasks

- [x] 1.1 Schema: `payees` table (name, default category, optional default financial account) - `lib/data/database/tables/payees_table.dart`, registered in `app_database.dart` (schemaVersion 12→13, `if (from < 13) await m.createTable(payees);`).
- [x] 1.2 CRUD (minimal, inline or in Settings) for payees - `PayeeManagementViewModel`/`PayeeManagementView` (add/rename/delete, real delete not archive since a payee is only a memory aid), reachable via a "Manage payees" button in Settings → `/payees` route.
- [x] 1.3 Record form: autocomplete on description using `normalizeDescription` (reused, not reimplemented); apply matched payee's defaults, overridable - `RecordTransactionViewModel.payeeSuggestions`/`selectPayee`, wired to an `Autocomplete<Payee>` around the description field in `record_transaction_view.dart`.
- [x] 1.4 Remember last-used account/category per payee after each recorded transaction - `LedgerRepository.recordPayeeUsage`, called from `RecordTransactionViewModel.submit` for the explicitly selected payee, or one whose name exactly matches the typed description even without an explicit selection.
- [x] 1.5 Import: when saving a category rule from a group assignment, offer to also link/create a payee named after the rule's keyword with the rule's category as default; declining leaves rule-saving unchanged - a pre-checked "Also remember as a payee" checkbox in the existing save-rule dialog, backed by `LedgerRepository.findOrCreatePayeeByName` and `StatementImportViewModel.linkPayeeToRule`.
- [x] 1.6 Tests: payee suggestion and override; last-account/category memory updates after recording; rule-save payee-linking offer, both accepted and declined paths - `ledger_repository_test.dart`'s "payees" group, `record_transaction_view_model_test.dart`'s "payees-and-spending-memory" group, `record_transaction_view_test.dart`'s Autocomplete widget test, `statement_import_view_test.dart`'s "link a payee from the save-rule dialog" group, `payee_management_view_test.dart`.
- [x] 1.7 User guide: payees, autocomplete, and the rule-save linking offer - Recording a transaction, Importing bank statements, and Settings sections.
