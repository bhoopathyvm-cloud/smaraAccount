## Tasks

- [x] 1.1 `HomeViewModel`: this-month category aggregates via a new `LedgerRepository.watchCategoryTotals` (grouped by category, home-hub-capture design.md Decision 1's "reuse the Summary query" premise didn't hold - `watchSummary` only ever collapsed into two totals, never by category, so a genuinely new aggregation query was needed; mirrors `watchSummary`'s own join/exclusion logic).
- [x] 1.2 Home Add action: a shared `showCaptureActionSheet` bottom sheet (`lib/ui/core/capture_action_sheet.dart`) offering Spent / Received / Moved money / Import statement, used by both Home and Register.
- [x] 1.3 This-month-by-category list on Home (`_ThisMonth`/`_CategoryTotalRow` in `home_view.dart`), hidden entirely when there's no activity either way this month.
- [x] 1.4 Register: replaced the three separate FABs with one `FloatingActionButton.extended` opening the same sheet as Home's Add, with the current account pre-selected via the existing router account-preselection logic; the archived-account disabled condition and the separate closeout affordance are unchanged.
- [x] 1.5 Tests: `watchCategoryTotals` repository tests (grouping, date-range exclusion, quarantine exclusion), `HomeViewModel` tests (expense/income split, sort order, current-month date range), `HomeView`/`RegisterView` widget tests (sheet choices, disabled-when-archived, callback wiring), `RecordTransactionViewModel` test for `initialDirection` (the Spent/Received pre-selection plumbed through the sheet and a new `direction` route query param).
- [x] 1.6 User guide: Home described as the primary capture surface with its Add button and This month section; Register's Add noted as opening the same choice with the viewed account pre-selected.
