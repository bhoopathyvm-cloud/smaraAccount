## Tasks

- [x] 1.1 Repository: `recordSplitTransaction` posts one financial-account leg plus one posting per category line; validates lines sum exactly to a caller-supplied `totalAmountMinor` (defense-in-depth beyond the UI's own remainder check) and every line has an active, direction-matching category before posting anything (`_requireActiveCategoryOfType`). No foreign-currency support for v1 (see design.md's "Filled in during implementation" - not in the original design, resolved during apply).
- [x] 1.2 Split UI on the record screen: `RecordTransactionViewModel.startSplitting`/`addSplitLine`/`removeSplitLine`/`setSplitLineCategory`/`setSplitLineAmount`, `splitRemainderMinor` live-computed getter; `record_transaction_view.dart`'s new `_SplitLines` widget, Save disabled while the remainder is nonzero. Removing down to one line collapses back to the ordinary single-category form (`categoryId`/`setCategoryId` unchanged, still the non-split path). Foreign-currency fields hidden while splitting.
- [x] 1.3 `RegisterViewModel`/`RegisterRow`: `counterpartAccountIds` (a list, was a single id) collects every non-financial-account posting; `_counterpartLabel` renders "first name +N more" for more than one, unchanged for exactly one.
- [x] 1.4 Validation: reject a split whose lines don't sum to the total, or where any line lacks an active, direction-matching category; the error message names which line failed (`Split line N (id) ...`).
- [x] 1.5 Confirmed (no code change, per design.md Decision 4) reversal and Summary already handle a split entry correctly - see the "recordSplitTransaction" repository test group's two regression tests.
- [x] 1.6 Tests: `ledger_repository_test.dart`'s "recordSplitTransaction" group (posting shape, validation for each failure mode, reversal, Summary); `register_view_model_test.dart`'s "split-transactions" group (multi-category label, `isRowFixable` false for a split, single-category unaffected); `record_transaction_view_model_test.dart`'s "split-transactions" group (state transitions, submit validation and posting); `record_transaction_view_test.dart`'s "split-transactions" group (UI: expand/collapse, remainder, Save gating, posting).
- [x] 1.7 User guide: recording a split (new "Splitting across multiple categories" subsection), that reversing a split reverses every line together, and the Fix flow excludes split rows.
